name: Deploy docs to Pages

on:
  push:
    branches: [stable]
    paths:
      - 'docs/**'
      - '.github/workflows/deploy-docs.yml'
  workflow_dispatch:

# GitHub Pages permissions
permissions:
  contents: read
  pages: write
  id-token: write

# Allow one concurrent deploy; don't cancel in-progress runs.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout texish
        uses: actions/checkout@v7
        with:
          path: texish

      # juicer lives in a sibling repo. Clone its stable branch so we
      # build with the same version juicer.run itself uses in production.
      - name: Checkout juicer
        uses: actions/checkout@v7
        with:
          repository: edadma/juicer
          ref: stable
          path: juicer

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up sbt
        uses: sbt/setup-sbt@v1

      - name: Set up Node (for sass + esbuild)
        uses: actions/setup-node@v6
        with:
          node-version: '20'

      - name: Install sass and esbuild
        run: npm install -g sass esbuild

      - name: Cache sbt
        uses: actions/cache@v5
        with:
          path: |
            ~/.sbt
            ~/.cache/coursier
            ~/.ivy2/cache
          key: ${{ runner.os }}-sbt-${{ hashFiles('juicer/**/build.sbt', 'juicer/**/project/build.properties', 'juicer/**/project/plugins.sbt') }}
          restore-keys: |
            ${{ runner.os }}-sbt-

      - name: Build docs
        working-directory: juicer
        run: |
          sbt --error "juicerJVM/run build -s ../texish/docs -d ../texish/docs/_site"

      # GitHub Pages requires a CNAME file in the site root to route the
      # custom domain. Generated here rather than checked in so the source
      # tree stays clean.
      - name: Add CNAME for texish.edadma.dev
        run: echo "texish.edadma.dev" > texish/docs/_site/CNAME

      - name: Setup Pages
        uses: actions/configure-pages@v6

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: texish/docs/_site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
