name: Release binaries

# Builds the native texish CLI and attaches it to a GitHub release. Runs
# automatically when a release is published, and can be dispatched manually
# against an existing tag (e.g. to add binaries to a release made by hand).
on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag:
        description: 'Existing release tag to attach binaries to (e.g. v0.10.0)'
        required: true

# gh release upload needs write access to releases.
permissions:
  contents: write

jobs:
  build:
    name: ${{ matrix.os }} ${{ matrix.arch }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # Build Linux on 22.04 (glibc 2.35) so the binaries also run on older
          # distros; newer glibc is not backward-compatible, older is forward-compatible.
          - { runner: ubuntu-22.04,     os: linux, arch: x86_64 }
          - { runner: ubuntu-22.04-arm, os: linux, arch: arm64  }
          # Pin to an explicit macOS image (not macos-latest) so the binary's
          # deployment-target floor stays low and predictable instead of drifting
          # up whenever GitHub moves the rolling label to a newer macOS.
          # macOS Intel (macos-13) is omitted: that runner pool is so scarce the job
          # would sit unassigned for the whole run, leaving the action stuck "queued"
          # and never attaching the other binaries. Intel Macs fall back to the JVM build.
          - { runner: macos-14,         os: macos, arch: arm64  }
    steps:
      - name: Checkout texish
        uses: actions/checkout@v7

      # Scala Native links through the host's C/C++ toolchain, and the CLI binds
      # cairo, freetype and turbojpeg, so those system libraries must be present
      # at link time. macOS runners already have clang + these libs; Linux needs
      # them installed.
      - name: Install native toolchain (Linux)
        if: matrix.os == 'linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y clang zlib1g-dev libcairo2-dev libfreetype-dev cmake

      # The turbojpeg binding uses the TurboJPEG 3 API (tj3*), which only exists
      # in libjpeg-turbo 3.x. Ubuntu's apt package is still 2.x, so build 3.x from
      # source and install it into the default prefix where -lturbojpeg finds it.
      - name: Build libjpeg-turbo 3.x (Linux)
        if: matrix.os == 'linux'
        run: |
          git clone --depth 1 --branch 3.0.4 https://github.com/libjpeg-turbo/libjpeg-turbo.git
          cmake -S libjpeg-turbo -B libjpeg-turbo/build \
            -DCMAKE_BUILD_TYPE=Release -DCMAKE_INSTALL_PREFIX=/usr -DWITH_SIMD=0
          cmake --build libjpeg-turbo/build -j
          sudo cmake --install libjpeg-turbo/build
          sudo ldconfig

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up sbt
        uses: sbt/setup-sbt@v1

      - name: Cache sbt
        uses: actions/cache@v5
        with:
          path: |
            ~/.sbt
            ~/.cache/coursier
            ~/.ivy2/cache
          key: ${{ runner.os }}-${{ runner.arch }}-sbt-${{ hashFiles('build.sbt', 'project/build.properties', 'project/plugins.sbt') }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-sbt-

      - name: Build native CLI (release-fast)
        run: sbt 'set ThisBuild / nativeConfig ~= { _.withMode(scala.scalanative.build.Mode.releaseFast) }' texishCli/nativeLink

      # Asset name drops the leading "v" so it matches the artifact version
      # (texish-0.10.0-linux-x86_64, not texish-v0.10.0-...).
      - name: Resolve version
        id: ver
        run: |
          TAG='${{ github.event.release.tag_name || inputs.tag }}'
          echo "version=${TAG#v}" >> "$GITHUB_OUTPUT"
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"

      - name: Stage binary
        run: cp cli/target/scala-*/texish-cli "texish-${{ steps.ver.outputs.version }}-${{ matrix.os }}-${{ matrix.arch }}"

      - name: Upload to release
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release upload "${{ steps.ver.outputs.tag }}" "texish-${{ steps.ver.outputs.version }}-${{ matrix.os }}-${{ matrix.arch }}" --clobber

  # The binaries carry only the compiled-in core, so on their own they cannot set Hebrew, resolve
  # \use{diagram}, or hyphenate anything but the five languages compiled in. This job attaches the
  # rest: the font catalogue, the packages and the hyphenation patterns, as one platform-independent
  # archive. No matrix, no toolchain — a checkout and a tar.
  share:
    name: share tarball
    runs-on: ubuntu-latest
    steps:
      - name: Checkout texish
        uses: actions/checkout@v7

      - name: Resolve version
        id: ver
        run: |
          TAG='${{ github.event.release.tag_name || inputs.tag }}'
          echo "version=${TAG#v}" >> "$GITHUB_OUTPUT"
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"

      # The archive carries its own share/texish/ prefix, which is one of the two layouts
      # Install.homeNear searches for. Unpacking it at an install prefix therefore lands the tree
      # exactly where the binary in that prefix's bin/ will look, with nothing left to move.
      - name: Stage the tree
        run: |
          mkdir -p stage/share/texish
          cp -R fonts packages hyphenation stage/share/texish/

      # Built deterministically — fixed member order, zeroed timestamps and ownership, and a gzip
      # header with no embedded mtime — so re-running this workflow against an existing tag produces
      # a byte-identical archive. A package manager's formula pins the checksum, and --clobber would
      # otherwise replace the asset with an equivalent one whose hash no longer matches.
      - name: Build the tarball
        id: tar
        run: |
          NAME="texish-${{ steps.ver.outputs.version }}-share.tar.gz"
          tar --sort=name --mtime='UTC 1970-01-01' --owner=0 --group=0 --numeric-owner \
              -I 'gzip -n' -cf "$NAME" -C stage share
          sha256sum "$NAME" > "$NAME.sha256"
          echo "name=$NAME" >> "$GITHUB_OUTPUT"

      # The checksum is what a formula pins, so surface it where it can be copied out of the run
      # rather than only inside the uploaded file.
      - name: Report
        run: |
          {
            echo '### Share tarball'
            echo
            echo "\`$(cat "${{ steps.tar.outputs.name }}.sha256")\`"
            echo
            echo "$(du -h "${{ steps.tar.outputs.name }}" | cut -f1) compressed"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload to release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release upload "${{ steps.ver.outputs.tag }}" \
            "${{ steps.tar.outputs.name }}" "${{ steps.tar.outputs.name }}.sha256" --clobber

  # A release is not finished until `brew install` gives you the new version: the jobs above are how
  # texish is downloaded, but the formula is how it is installed. This renders the formula from the
  # assets they just attached — it needs all four, so it waits for both — and pushes it to the tap.
  # Without the tap token it still renders and prints the formula, so a release never fails for want
  # of a secret and the result can always be copied in by hand.
  formula:
    name: homebrew formula
    needs: [build, share]
    runs-on: ubuntu-latest
    env:
      TAP_TOKEN: ${{ secrets.TAP_TOKEN }}
    steps:
      - name: Checkout texish
        uses: actions/checkout@v7

      - name: Resolve version
        id: ver
        run: |
          TAG='${{ github.event.release.tag_name || inputs.tag }}'
          echo "version=${TAG#v}" >> "$GITHUB_OUTPUT"

      - name: Render the formula
        run: tools/brew-formula.sh "${{ steps.ver.outputs.version }}" -o texish.rb

      - name: Report
        run: |
          {
            echo '### Homebrew formula'
            echo
            echo '```ruby'
            cat texish.rb
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

      # A tap is an ordinary repository, so this is a clone, a copy and a push — no Homebrew involved,
      # and none of the ownership trouble that comes with running brew itself. TAP_TOKEN is a token
      # with contents:write on edadma/homebrew-tap; the default GITHUB_TOKEN cannot reach another
      # repository.
      - name: Push to the tap
        if: env.TAP_TOKEN != ''
        run: |
          git clone --depth 1 "https://x-access-token:${TAP_TOKEN}@github.com/edadma/homebrew-tap.git" tap
          mkdir -p tap/Formula
          cp texish.rb tap/Formula/texish.rb
          cd tap
          if git diff --quiet -- Formula/texish.rb; then
            echo "the tap already carries this formula — nothing to push"
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
          git add Formula/texish.rb
          git commit -m "texish ${{ steps.ver.outputs.version }}"
          git push

      - name: Note a missing token
        if: env.TAP_TOKEN == ''
        run: |
          echo "::warning::TAP_TOKEN is not set, so the formula was rendered but not pushed to edadma/homebrew-tap. It is in this run's summary; copy it to Formula/texish.rb there."
